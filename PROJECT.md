# 芝麻 (Zhima Agent)

> 一颗芝麻虽小，但能榨出油。
>
> 一个桌面操作虽碎，但能被自动化。

## 项目背景

芝麻源自 [UI-TARS Desktop](https://github.com/bytedance/ui-tars-desktop)（ByteDance 开源的 GUI Agent 桌面端），基于其核心的视觉-语言模型驱动能力进行独立演进。

UI-TARS 提供了一套通过视觉理解来操作电脑的基础框架，但作为开源项目，它的更新节奏已放缓，且原生构建链路存在缺陷（bytecodePlugin 导致生产环境白屏）。芝麻在此基础上做了修复，并追加了 NSIS 安装包支持，后续将沿着自己的路线发展。

## 定位

**不是"AI 助手"，而是"桌面自动化执行器"。**

区别于 Claude Code Desktop 和 Codex Desktop 这类通用 AI 助手，芝麻的核心定位是：让用户通过录制+人工干预的方式，教会 AI 执行复杂的、多步骤的专业软件操作流程。

## 目标场景

通用 GUI Agent 擅长"打开网易云音乐并播放"这类简单任务，但在以下场景力不从心：

- **专业设计软件**：Photoshop 批量处理、3D 建模软件中的重复操作
- **办公软件**：Word/WPS 批量排版、Excel 复杂数据处理流程
- **企业系统**：没有 API 的老旧系统、内部 OA 流程操作
- **测试验收**：桌面应用的自动化测试、UI 回归验证

这些场景的特点是：操作链路长、步骤机械重复、但路径相对固定。纯靠模型推理无法稳定完成，但通过"人演示 + AI 学习 + 人纠错"的循环，可以逐步固化为自动化工作流。

## 核心竞争力

1. **工作流录制与积累** — 用户通过文字 + 截图演示操作，AI 严格按流程执行。遇到边界情况时人介入修正，流程逐步优化。随着时间推移，沉淀出行业专属的工作流库。
2. **模型无关** — 支持本地模型（Ollama/Qwen 等），数据不出域，适合企业私有化部署。不受任何一家大模型厂商锁定。
3. **垂直场景深耕** — 大厂做通用的 AI 桌面助手，芝麻做特定软件的深度自动化。路线不同，不正面竞争。

## 发展路线

```
开源社区版（免费）
  ├── 核心 Agent 能力（鼠标键盘操控、视觉理解）
  ├── 工作流录制与回放
  ├── 本地模型支持
  └── 个人日常使用

企业版（付费）
  ├── 工作流模板库（云端同步与分享）
  ├── 团队协作（多人共享与维护工作流）
  ├── 私有化部署 + 管理后台
  └── 企业系统集成（钉钉/飞书/企业微信等）
```

## 当前状态

- 基于 UI-TARS Desktop 最新代码
- 已修复：bytecodePlugin 导致的生产白屏问题
- 已修复：renderer 资源路径问题（base: './'）
- 已新增：NSIS 安装包支持（自定义安装路径）
- 已修复：命名一致性问题（应用名、可执行文件、快捷方式统一）
- 功能层面仍需大量迭代

## 关于长期价值

AI 模型能力的提升对芝麻不是威胁，而是杠杆。模型越强，能理解和执行的流程就越复杂，芝麻所能覆盖的场景就越广。那些机械化的专业软件操作流程，本质上承载的是行业特有的业务知识，而不是通用推理能力——后者是模型擅长的，前者正是芝麻要积累的。
