{
  "private": true,
  "name": "agent-tars",
  "version": "0.3.0",
  "description": "Agent TARS",
  "scripts": {
    "bootstrap": "pnpm --filter \"./tarko/**\" build && pnpm --filter \"./gui-agent/**\" build && pnpm --filter \"./agent-tars/**\" build && pnpm --filter \"./omni-tars/**\" build",
    "docs": "doctoc --title '## Table of Contents' README.md",
    "clean": "pnpm run -r clean",
    "dev": "pdk d",
    "dev:core": "pdk d --packages @agent-tars/core",
    "build": "pnpm run -r build",
    "build:tarko": "pnpm --filter \"./tarko/**\" --include-dependencies build",
    "build:ui": "pnpm --filter @tarko/ui --filter @tarko/agent-ui build",
    "build:agent-tars": "pnpm --filter \"./agent-tars/**\" --include-dependencies build",
    "build:omni-tars": "pnpm --filter \"./omni-tars/**\" --include-dependencies build",
    "build:gui-agent": "pnpm --filter \"./gui-agent/**\" --include-dependencies build",
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage",
    "format": "pnpm prettier --write .",
    "release": "pdk release",
    "release:dryrun": "pdk release --dry-run",
    "release:canary": "pdk release --canary",
    "release:full": "pdk release --create-github-release",
    "release:ai": "pdk release --use-ai",
    "release:ai:full": "pdk release --use-ai --create-github-release",
    "release:ai:dryrun": "pdk release --use-ai --dry-run",
    "github-release": "pdk github-release",
    "github-release:dryrun": "pdk github-release --dry-run",
    "changelog": "pdk changelog",
    "patch": "pdk patch"
  },
  "engines": {
    "node": ">=22",
    "pnpm": "9"
  },
  "devDependencies": {
    "pnpm-dev-kit": "0.0.5-canary-7d05b7ce-20251213170600",
    "@types/node": "22.15.30",
    "@vitest/coverage-v8": "3.2.4",
    "doctoc": "2.2.1",
    "prettier": "3.5.3",
    "typescript": "5.8.3",
    "vitest": "3.2.4",
    "yaml": "2.8.0",
    "tsx": "^4.19.2"
  }
}
